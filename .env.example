# Django
SECRET_KEY=change-me
DJANGO_ALLOWED_HOSTS=api.trashpanda.vladpaun.com
CSRF_TRUSTED_ORIGINS=https://api.trashpanda.vladpaun.com
MAX_UPLOADS_PER_PRINCIPAL=10
UPLOAD_RETENTION_HOURS=24
VOSK_MODEL_DIR=/models
API_HOST=api.trashpanda.vladpaun.com
WEB_HOST=trashpanda.vladpaun.com

# Database (used by Django and the Postgres container)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=trash_panda
DB_USER=trash_panda
DB_PASSWORD=change-me
DB_HOST=db
DB_PORT=5432
POSTGRES_DB=trash_panda
POSTGRES_USER=trash_panda
POSTGRES_PASSWORD=change-me

# Cache / queues
REDIS_URL=redis://redis:6379/0

# Traefik / TLS (match your environment)
TRAEFIK_CERTRESOLVER=cf-dns
